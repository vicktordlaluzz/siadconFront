import { BusquedaClientePipe } from './busqueda-cliente.pipe';

describe('BusquedaClientePipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaClientePipe();
    expect(pipe).toBeTruthy();
  });
});
